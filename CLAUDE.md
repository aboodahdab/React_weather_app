# Weather App

## About
React weather app built to practice skills from supersimpledev's course. Stack: Vite 8 + React 19 + TypeScript + Tailwind CSS v4.

## User preferences
- The user is learning — keep all responses short and to the point.
- Do NOT give extra informatio
n unless explicitly asked.
- When answering questions, be generic — do NOT show how to apply the answer in the code unless asked.
- Never read the `.env` file.
- When comparing options, always include pros and cons for each.
- When asked for a design, always create it as an HTML file — never in React unless explicitly told to.
- When asked to "check the code", only look at what is currently being discussed — do not audit everything.
- Do not point out issues or extra information unless explicitly asked.